import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  container: {
    margin: 0,
    paddingLeft: "1rem",
    paddingTop: "1rem",
  },
  loading: {
    fontSize: "1.5rem",
  },
});
